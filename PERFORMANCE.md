# 0.4.2 性能排查与验证

2026-09-22，Windows 同机对比 0.4.1（提交 `d7af15a6c2d21c8b9bf6b2efd09c159e55111fc4`）。测试使用独立配置、本机临时文件与回环 SSH 服务，没有连接用户服务器。以下是各阶段的测量，不能直接换算为真实网络下的整体性能。

## 已实施的优化

| 场景 | 原因与方案 | 原来 | 优化后 |
| --- | --- | ---: | ---: |
| 一万条文件首次显示 | 全量行绘制改为只绘制可视区及附近条目 | 4,023 ms | 22 ms |
| 同目录单次选择 | 缓存日期格式器、选中集合与行组件 | 1,867–2,734 ms | 约 7 ms |
| 传输进度更新时的大目录界面 | 隔离列表行，避免无关更新重绘 | 1,991–4,455 ms | 3–6 ms |
| 同目录排序 | 完整数据排序，只绘制可视行 | 3,373 ms | 35 ms |
| 8 MiB 终端输出的字节解码 | 逐字节回调改为原生 Base64 解码 | 392 ms | 5.2 ms |
| 8 MiB 本机真实 SFTP 下载阶段 | 顺序等待改为最多 16 个预读、按序写入 | 199 ms | 152 ms |
| 同样下载，每个读取回应额外延迟 40 ms | 同上，用于观察等待时间的影响 | 5,944 ms | 438 ms |

目录测试使用实际 Electron、App 和 React 开发模式，同一份模拟目录；测量从数据/操作送到界面至提交与布局完成，不含远程目录读取。可视区绘制行从 10,000 减为 28，目录面板内元素从 140,062 减为 457。其余滚动位置的数量随视口变化。批量选择 4,999 项后右键菜单使用 Set 查找，实测打开约 8 ms；完整选择仍包含屏幕外条目。

解码结果为 Electron 中 7 轮交替测量的中位数，每轮处理 8 MiB、每包 32 KiB，只包含 Base64 转字节的 CPU 时间。没有原生方法时的直接循环兼容路径约 22 ms。中文、emoji 和控制序列的完整字节流与原实现一致。此结果**不是按键到屏幕延迟**，也不意味着整个终端快 75 倍。

下载结果为真实 ssh2 与 AsyncSSH 回环服务的 3 轮交替中位数。40 ms 是在每次 READ 回应上人为加入等待，不是实际广域网测速；这一组包含校验的完整任务中位数为 6,228 → 678 ms。下载预读数据固定最多 1 MiB，写入始终按顺序进行，临时文件保持可续传的连续前缀。

另有两项结构性改进：

- 12 个终端标签和主界面共享一次跨桥事件订阅，原先每个事件跨桥 13 次，现在 1 次；事件次序、输出解析和流控确认保留。
- 本地目录最多同时进行 32 个元数据读取，避免一万条目录同时挂起一万个文件请求；仍返回完整目录，不承诺所有磁盘上的读取时间都会缩短。

0.4.1 已优化传输校验：可用时在远端计算完整 SHA-256，仅回传摘要；不支持时并发回读。0.4.2 继续保留该机制，未改成抽样或只比较文件大小。

## 正确性验证

- 默认测试及真实 SFTP、WSL Python 组合：301 项通过，32 项因环境或需要单独启用而跳过，0 失败。
- 文件管理原有 34 个界面行为检查通过；新增大目录回归覆盖跨屏 Shift 选择、完整拖拽数据、批量右键、排序保留选中、滚动到底、独立折叠、隐藏时刷新和切换目录重置滚动。
- Chromium 原生拖放另行通过：拖动源行滚出可视区并卸载后，两栏间上传和下载仍携带正确路径及会话。使用原生鼠标启动与 CDP 拖放，未覆盖 Windows 资源管理器跨应用 OLE 拖放。
- 真实 Electron 终端验证后台标签完整接收并确认 1,116,060 字节，中文、备用屏幕、粘贴模式和切回内容保持；GPU 多标签像素及订阅生命周期检查通过。重连的 WebGL/DOM 两条路径和多行粘贴回归通过。
- 下载覆盖短读、乱序回应、前缀续传、源文件变化、取消、断线及完整内容校验。真实 SSH 取消及断线后重新连接续传通过，保留的临时文件为正确连续前缀。
- 修复 macOS 公钥安装测试的进程启动方式：隔离 POSIX fixture 显式采用 fork，避免默认 spawn 无法加载动态 fixture。对应本机 WSL 回归通过，macOS 原生结果以本次 CI 为准。

## 复现入口

在 `desktop/` 安装依赖后：

```powershell
# 默认测试 + 临时本机 AsyncSSH 服务；Python 环境需安装 tests-support/requirements.txt
node scripts/ci-tests.mjs

# Electron 文件、粘贴、多标签、目录性能及输出解码回归（使用隔离配置）
node scripts/ci-workspace-tests.mjs

# 额外原生拖放边界验证
$env:GOOESHELL_NATIVE_DRAG_TEST = '1'
node node_modules/tsx/dist/cli.mjs --test tests/explorer-native-drag.test.ts

# 可选：在同样的临时服务中加入真实下载对比，基准源码从上述固定提交读取
$env:GOOESHELL_DOWNLOAD_BENCHMARK = '1'
node scripts/ci-tests.mjs
```

可用 `GOOESHELL_TEST_PYTHON` 指定 Python。目录与终端报告位于 `.build/explorer-performance-*`、`.build/terminal-output-*`，下载对比位于 `desktop/test-output/sftp-download-benchmark.json`。耗时作为诊断记录，CI 使用行为、字节一致性和资源数量断言，不用固定毫秒阈值判断不同机器快慢。

## 后续方向与本轮边界

本轮先消除已测得的重复工作和等待。上传暂时保留顺序写入：若直接并发写远端，断线时可能留下有空洞的文件，需要先设计可靠的恢复元数据，不能只追求吞吐量。隐藏终端仍解析后台输出，xterm 已暂停隐藏区域绘制；不丢弃输出、不暂停流控确认。

真实公网的按键到屏幕延迟、用户服务器的磁盘和 CPU 瓶颈，以及并行上传的收益尚未实测。后续优先按实际慢场景分别测量网络往返、远端摘要计算和绘制耗时，再决定是否需要可调预读窗口或上传恢复机制。
