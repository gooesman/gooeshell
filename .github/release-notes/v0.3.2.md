gooeshell 0.3.2：更鲜明的终端颜色与可选粗体。

- 默认纯黑配色采用饱和度更高的蓝、绿等 ANSI 颜色，原版较浅的配色保留为“柔和”。
- 默认保持用户选择的中英文字重，不因终端输出的强调标记额外加粗；可在“设置 → 终端配色”开启“允许终端文字额外加粗”。
- 加粗不再自动将普通颜色替换为亮色；程序明确指定的亮色、256 色和真彩色继续按原有含义显示。
- 调整设置直接更新已有终端，保留滚动内容和终端模式。

沿用原应用数据目录，已有连接、跳板机及凭据继续保留。
