# Shell 工具需求与技术验证草案

状态：技术栈确定为 Rust + WezTerm 二次开发；待原型验证性能及兼容性；当前仅确定方案与功能边界，尚未开始应用实现或性能测试。
日期：2026-09-10
平台：用户已确认先做好 Windows，保留跨平台可能。

## 产品目标

制作个人使用的 SSH 终端和远程文件管理工具。第一优先级为交互延迟，其次为传输可靠性、终端兼容和界面简洁。默认纯黑背景，各功能栏可以隐藏。暂不加入服务器监控、云同步等未提出的功能。

## 功能范围

| 模块 | 需求与边界 |
| --- | --- |
| 连接 | 保存主机配置；SSH 密码、公钥认证；断线状态明确可见 |
| 字体 | 参考用户提供的 FinalShell/Xshell 字体截图；中英文字体分别设置、家族/字重/字号、实时预览、回退字体；系统字体优先；详细约定见 FONT-KEYBOARD.md |
| 编码 | 每个连接指定终端编码，默认 UTF-8；传统编码采用有状态的双向转换，正确处理跨包字符与终端控制序列；文件内容和文件名编码单独处理 |
| 键盘 | 作为重点功能：按焦点和上下文匹配、自定义与冲突检查、录入组合键、恢复默认、导入导出；保留终端 Ctrl+C/Ctrl+R 等语义；支持中文输入法、组合键和括号粘贴；详细约定见 FONT-KEYBOARD.md |
| 远程文件 | 路径导航、渐进显示目录、上传下载、新建、重命名、删除、属性；大目录只绘制可见行；不默认递归扫描或逐文件预取 |
| 传输 | 大文件上传和下载的暂停、取消、重连与恢复；应用重启后恢复；进度、速率、队列及可调限速 |
| 历史 | 远程 Shell 原有上下键历史继续工作；提供独立的可搜索历史面板及按主机筛选、收藏、删除、禁用记录；优先使用 Shell 集成识别完整命令，不能把原始按键当命令日志 |
| 主机指纹 | 永久信任、仅本次信任且不写入磁盘；不保存不等于静默跳过校验；已有记录发生变化时单独处理 |
| 公钥部署 | 使用已认证连接部署用户选择的公钥；仅上传公钥；幂等追加、不覆盖已有 authorized_keys；正确处理权限；用新连接验证认证成功后报告完成；首版以常规 Linux/OpenSSH 为目标 |
| 外观 | 默认纯黑；可选本地背景图片及显示方式；无需每帧重新解码图片 |
| 栏位 | 标签栏、连接栏、文件栏、状态栏独立隐藏，保存布局，快捷键恢复 |
| 显示模式 | 普通窗口；窗口内纯终端；无边框桌面全屏；用户已确认无需显卡独占全屏；提供快捷键退出并恢复先前布局 |

## 性能设计约束

- 分开测量客户端处理、网络往返、服务器回显和屏幕呈现；不能承诺跨公网零延迟。
- 输入队列与文件传输队列分离。后台任务不得在界面线程等待网络或磁盘。
- 终端输出可批量处理，但不能用长时间定时器积攒键盘输入。
- 队列与历史缓冲设置边界；输出过载采用流量控制，不静默丢弃终端字节。
- SFTP 优先使用独立 SSH 连接，与终端避免共用单一阻塞锁；独立连接仍会争用物理带宽，需要并发和速率控制。
- 文件列表虚拟化；传输进度降频刷新；隐藏面板暂停无必要的界面更新。
- 不通过简单的本地回显伪装低延迟，避免密码输入、Vim、方向键和服务器回显出现错误。

## 断点续传约定

1. 分块读写，文件大小和偏移使用 64 位整数，不将整文件载入内存。
2. 持久化任务标识、服务器身份、源路径、目标路径、暂存路径和检查点；凭据不混入任务日志。
3. 上传使用可识别的暂存文件；下载使用本地暂存文件；提交最终文件前应用明确的同名处理策略。
4. 恢复时重新认证并核实源与暂存目标。大小和修改时间只能发现部分变化，不能证明内容一致；必要时验证已传前缀或分块哈希，无法验证时明确选择重新传输或确认。
5. 不能把曾经送入发送队列的字节数视为已经可靠写入的断点；恢复时核实实际状态。
6. 完成后验证内容；原子改名取决于服务器能力和文件系统，不能一概承诺。
7. 验证上传和下载两条路径，覆盖断网、进程退出、目标冲突、源文件改变、磁盘满和权限不足。

## 已选技术栈

方案：基于 WezTerm 的 Rust 原生应用二次开发，保留原有终端、字体、输入和渲染链路，在应用内增加可隐藏的文件管理与设置面板。需要修改源码，不能仅靠 Lua 配置完成整套产品。新增业务模块独立组织，减少对上游核心的修改，便于跟进兼容修复。

| 层次 | 选择 | 实现边界 |
| --- | --- | --- |
| 主语言 | Rust | 新增产品逻辑与后台任务 |
| 终端 | WezTerm 源码中的终端与输入模块 | 保留终端兼容、输入法、字体排版和滚动；固定上游提交后开发 |
| 界面与窗口 | WezTerm 原生 GUI、窗口模块及 wgpu 渲染链路 | 扩展文件、历史和设置面板；文件表格只绘制可见行；使用现有全屏机制验证 Windows 无边框行为 |
| SSH/SFTP | wezterm-ssh，选定 ssh2/libssh2 后端 | 上游同时提供 libssh-rs 和 ssh2 后端；第一版统一选择一个；文件传输使用独立连接及工作队列 |
| 后台执行 | 沿用上游 smol/任务机制，文件任务放独立工作线程 | 不把同步 SSH、磁盘读写或数据库操作放入界面线程；用有界队列传递进度 |
| 持久化 | SQLite | 命令历史与传输检查点；凭据不写入任务日志 |
| 产品设置 | Serde + JSON | 可视化设置界面负责读写；保留上游 Lua 能力用于高级配置，但普通使用不要求编辑 Lua；产品设置合并优先级明确 |
| 编码 | encoding_rs + 终端流适配层 | 默认 UTF-8；计划支持 GBK、Big5 等库可支持的双向转换；必须验证控制序列与跨包字符，不能承诺完整 GB18030 编码能力 |

具体依赖版本在首轮构建时按上游锁文件固定。字体、窗口、渲染优先作为完整链路复用；不将内部 Rust crate 当作已有稳定外部 GUI 控件。

选择理由：用户强调输入延迟，又希望保留跨平台可能，WezTerm 已提供跨平台 Rust GPU 终端及字体、背景等基础。文件管理界面扩展和编码转换仍是实际开发成本，并非已有现成功能。无边框全屏不再限制方案选择；继续选择此路线的主要依据是复用完整原生终端链路。Windows Terminal + OpenSSH 作为 Windows 性能与交互对照。

优先研究已有原生 GPU 终端项目的二次开发，复用终端兼容、输入法、字体与渲染；不把终端协议解析器误认为完整可嵌入的终端控件。

| 路线 | 可复用部分 | 主要待验证问题 |
| --- | --- | --- |
| Windows Terminal 二次开发 | Windows 输入法、终端内核、字体、背景图、设置、ConPTY | Windows 限定；代码和构建复杂；文件面板整合、传统编码需要验证；不假定内部控件有稳定外部接口 |
| WezTerm 二次开发（已选） | Rust、跨平台 GPU 终端、字体、键盘、背景和窗口管理 | 添加文件列表与设置界面的工作量；Windows 实测延迟；传统编码需要适配 |
| Tauri + xterm.js + Rust | 终端组件、Web 界面生态、原生后台任务 | WebView 与跨进程链路需实测；背压和流式二进制传输；传统编码适配；满足无边框全屏需求 |

当前不因语言或 GPU 标签预先宣布某条路线最快。原型验收仍需覆盖延迟、中文输入、终端兼容与窗口模式；真正显卡独占全屏已移出范围。

## 开发顺序与验收

1. 技术验证：可连接的终端、中文输入、Vim/tmux、长输出、窗口模式；与同机 Windows Terminal + OpenSSH 进行同条件对照。
2. 文件功能：目录列表、上传下载、独立连接、传输时终端交互、持久化恢复及故障测试。
3. 日常功能：字体编码、快捷键、历史、公钥部署、指纹策略、背景图和布局保存。
4. 发布准备：固定依赖版本与许可证清单，安装包与配置迁移，依据测试结果决定默认配置。

性能报告同时提供闲置、持续日志、传输和大目录场景的 P50/P95/P99、内存和 CPU；客户端内部时间戳与实际按键到屏幕时间分开报告。当前没有测量数据，不宣称达到任何毫秒指标。

## 调研来源

- Windows Terminal：https://github.com/microsoft/terminal
- WezTerm：https://wezterm.org/ 与 https://github.com/wezterm/wezterm
- WezTerm 编码说明：https://github.com/wezterm/wezterm/blob/main/docs/escape-sequences.md
- xterm.js 编码：https://xtermjs.org/docs/guides/encoding/
- xterm.js 流量控制：https://xtermjs.org/docs/guides/flowcontrol/
- Tauri 流式通道：https://tauri.app/develop/calling-frontend/
- libssh2 非阻塞 SFTP：https://libssh2.org/examples/sftp_write_nonblock.html
- libssh2 64 位定位：https://libssh2.org/libssh2_sftp_seek64.html
- OpenSSH 主机校验：https://man.openbsd.org/ssh_config
- WezTerm GUI 依赖：https://github.com/wezterm/wezterm/blob/main/wezterm-gui/Cargo.toml
- WezTerm SSH 后端：https://github.com/wezterm/wezterm/blob/main/wezterm-ssh/Cargo.toml
- WezTerm 全屏：https://wezterm.org/config/lua/keyassignment/ToggleFullScreen.html
- 编码库：https://docs.rs/encoding_rs/latest/encoding_rs/
