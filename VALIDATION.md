# 验证与已知边界

## 0.4.1：传输校验提速与独立进度

普通与打包传输共用新的完整内容校验：256 KiB 及以上优先同时在本地和远端流式计算 SHA-256，远端仅回传摘要与进度。续传仍核对全部已有前缀，完成后仍核对完整文件，没有改成仅比较大小或抽样。文件变化、长度不足、摘要不一致会失败并保留普通传输的 `.gooeshell.part`，不会发布最终目标。

远端使用独立无终端 SSH exec 与固定 Python 3 程序，路径以 JSON 标准输入传入。在相同目录排他创建随机名、0600 的小型探针，hash 前后确认 SFTP 与 exec 命名空间一致；目标以非阻塞且不跟随链接的方式打开，核对读取前后文件与路径的身份、大小和纳秒时间。探针只清理当前操作创建的确切路径，断线时可能留下 `.gooeshell-verify-*` 小文件。没有 Python/exec 能力、目录不可写或路径映射不一致时自动回读；取消和内容错误不作为能力不足吞掉。

兼容回读固定最多 16 个 64 KiB 比较任务，等待已开始的读取完成后才关闭句柄，内存有界。能力不可用时及时停止本地摘要计算，避免完整读盘后再重复回读。校验界面显示“校验已有内容”或“校验文件”、字节数和百分比；准备阶段使用不定进度，校验字节不混入传输速度或预计剩余传输时间。

最终默认测试与隔离 SFTP、WSL Python 组合回归 283 项通过、30 项按环境跳过。包含真实 Python 摘要与路径探针整合、双向完整/前缀校验、内容不一致不发布、只支持 SFTP 的实际 SSH 服务回退、短读与并发错误、取消与迟到清理。Electron 文件管理 34 个行为检查通过；生产编译和 Windows 0.4.1 成品启动检查通过。

受控性能对比固定旧版本提交 `3015fff30dbf610e62de51aa56c659a0ddbed023`：8 MiB 文件，每次 SFTP READ 回应人为延迟 40 ms，旧顺序校验 5950 ms、新远端摘要校验 154 ms、16 并发回读 380 ms；对应校验阶段远端文件回读分别为 8 MiB、0、8 MiB。测试使用本机磁盘和真实 WSL Python，包含进程启动；这是延迟模拟，不代表用户服务器实测速度。可设置 `GOOESHELL_HASH_LINUX_TEST=1`、`GOOESHELL_HASH_BENCHMARK=1` 运行 `tests/transfer-hash-integration.test.ts` 复现。

## 0.4.0：登录身份、公钥推送与界面整理

侧栏服务器右键统一为“连接设置”，用户名归入身份验证，收藏、分组、图标、编码、指纹、独立 sudo、跳板机、公钥与连接管理收进默认折叠的高级选项。移除侧栏重复设置入口和顶部纯终端模式按钮，保留快捷键。设置分类合并“键盘与鼠标”，右键粘贴、选中复制和快捷键位于同页；身份页独立保存，保留其他设置草稿，设置窗口尺寸固定。

登录身份通过引用关联目标和跳板机，名称、账号、版本与加密长期密码原子保存，本次密码只在内存中。成功认证使用版本检查防止旧请求覆盖新密码，临时凭据不写回共享库；有关联时拒绝删除身份。现有会话和文件通道使用原登录快照，重连读取最新身份。共享身份与独立 sudo 密码的保存策略分开，系统加密暂不可用时可临时输入登录密码，不把旧独立 sudo 凭据降级或清除。

公钥通过专用无终端连接安装，保留既有授权内容和限制选项，按密钥内容去重；远端 Python helper 锁定文件并检查链接、所有者及权限。验证使用独立连接和选定私钥，不回退到密码或 Agent；保持原跳板路线及会话指纹。应用验证结果时重新检查连接、共享身份版本及磁盘私钥，只有验证成功才可改为密钥登录。取消或断线不影响已有终端。新生成的私钥存于独立目录，Windows 在写入前限制 ACL。

最终默认测试 230 项通过、32 项按环境跳过。身份后端与既有凭据、连接记录专项 62 项通过；公钥安装及 SSH/跳板回归 18 项通过，包含真实 Windows 密钥 ACL、WSL Linux 授权文件脚本和回环 SSH 验证。Electron 界面累计 87 个行为检查通过：连接流程 31、完整应用 26、连接表单 11、设置与身份 13、公钥界面 6；另真实 Electron 主进程连接、凭据和 sudo 回归通过。公钥结果与身份编辑、简化菜单截图已核对。测试使用临时配置与隔离测试服务，不读取真实用户凭据或连接用户服务器。

全量回归发现 ssh2 1.17 的 Ed25519 生成器偶尔误删公钥真实的前导零字节，输出格式损坏。现在生成结果须通过公私钥解析、匹配与签名验证，最多重生成 8 次，通过前不写文件；合法含前导零的外部密钥仍可正常读取。确定性损坏、错配、重试上限及前导零回归通过；额外生成 5000 对有效密钥，拦截 15 对底层损坏输出，最终失败 0。公钥安装、主进程及身份组合 23 项通过。

公钥主进程验证凭证另通过 7 项专项，覆盖取消时序、迟到成功、有效期、仅使用一次、连接与身份变更、私钥文件替换、只修改目标连接和口令保存；切换无口令私钥时原登录密码可作为本次 sudo 密码保留。

上一版 CI 的测试问题同时修复：文件管理 fixture 在小屏 runner 上维持完整列视口并换算鼠标坐标；终端 GPU fixture 先完整写入报告再退出；macOS 默认菜单刷新正例改由真实菜单角色触发，应用保护后的原生键盘检查仍保留。Windows 1024×768 文件管理完整 31 项、刷新快捷键和终端 GPU 5 项通过；macOS 专属路径需由新的 CI 结果验证。

生产编译与 Windows 0.4.0 成品启动检查通过，成品验证字体读取、文件操作隔离及包内压缩模块。其他平台构建结果以对应 GitHub Actions 记录为准。

公钥安装当前要求 Linux/OpenSSH 与 Python 3，使用常规 `~/.ssh/authorized_keys`，不自动处理自定义授权来源或 sudo 安装。安装成功和密钥登录成功分别显示；取消或验证失败时已安装的公钥可能保留。共享身份首版仅支持密码，外部编辑器和多级跳板不在本次范围。

## 0.3.12：重启后的密码输入提示

“本次使用期间”凭据仅保存在内存中，重启后重新输入属于预期。此前直接连接遇到缺少密码时，将 `AUTH_REQUIRED` 及 Electron 包装原样显示在身份验证窗口，输入过程中仍保留红字，容易误认为新输入的密码失败。

首次缺少目标或跳板机密码时正常打开验证窗口；只有提交后的失败或实际身份验证失败才显示错误，并去除技术包装。编辑密码或记忆方式后清除上一次认证错误，系统存储读取异常仍保留。三种记忆方式附带生效范围说明，未保存密码时阻止空表单提交，保留已保存密码的留空复用与可选私钥口令。用户的记忆方式及已有凭据不作自动更改。

凭据存储既有测试及冷启动隔离检查共 18 项通过，确认本次记忆在重启后消失、长期记忆可恢复、新输入密码正确传递，且解密异常使用独立错误。Electron 连接流程 27 个行为检查通过，覆盖真实输入／表单提交、空密码阻止、失败后编辑重试、跳板机、sudo 与标签归属；连接界面 8 个检查通过，并将遗漏的旧分组三点测试入口改为实际右键。认证截图核对、生产编译及 Windows 0.3.12 成品启动检查通过。测试不读取真实用户配置或连接用户服务器。

## 0.3.11：移除侧边栏分组三点按钮

展开和收起侧边栏都移除分组标题旁的三点按钮及其占位样式，悬停不再隐藏分组数量。右键分组标题或在标题聚焦后按 Shift+F10，仍可修改名称与图标、排序和删除分组；单击继续折叠／展开分组。

现有 Electron 连接管理回归 25 个行为检查通过，其中展开侧栏的排序／删除入口改为实际右键操作，折叠侧栏的名称与图标设置和分组删除路径继续通过。展开、折叠截图核对、生产编译与 Windows 0.3.11 成品启动检查通过。

## 0.3.10：精简文件工具栏与传输任务控制

移除本地／远程文件区的新建、删除、上传下载操作工具条，沿用右键菜单与拖拽入口；跟随终端合并到远程路径栏，两栏表头保持对齐。选中不新增提示行，空白点击取消选择。

传输任务显示近期实际有效载荷的速度与预计剩余时间；仅网络传输阶段显示，续传已有前缀、校验流量与压缩／解压不计入。速度使用单调时钟与 2 秒窗口，停滞后归零，阶段切换及任务结束清理定时器。压缩传输按压缩包字节估算，剩余时间不包含校验与解压耗时。

任务右键支持强制停止和删除任务。删除活动任务须先获得取消请求成功响应；请求失败保留任务供再次操作，删除已失败任务直接移除记录。删除记录同时清理重试参数，之后的旧进度事件不会恢复该记录；不调用文件删除接口。停止只中断任务自己的传输连接，保留终端会话。

后端专项 34 项全部通过，覆盖普通／压缩双向 SFTP、续传前缀不计速、延迟确认时速率降零并恢复、归档阶段映射、重复取消和终端连接保留，以及计时器释放。实际 Electron 文件界面 31 个行为检查通过，覆盖右键新建与删除、目录跟随、精简栏布局、速度／剩余时间更新、任务停止、删除等待取消、取消失败保留记录及旧事件隔离。截图核对、生产编译与 Windows 0.3.10 成品启动检查通过；所有传输测试使用私有回环测试服务器和临时目录。

## 0.3.9：多行粘贴前编辑命令

多行粘贴选择框改为可滚动的完整文本编辑区，支持修改、增删行；编辑区回车仅换行。整体粘贴与逐行模式均读取修改后的内容，未编辑时保留原始文本。清空后禁用两种发送按钮，控制器同时拒绝空内容提交。切换标签保留本连接的未提交草稿，返回后聚焦编辑区；取消、断线和重连清理待处理内容。Tab／Shift+Tab 在编辑区与可用按钮间循环，中文输入法组合过程中不抢占按键。

自定义终端快捷键处理后显式取消浏览器默认动作，避免快捷键粘贴重复触发原生粘贴；选择框打开时阻止底层终端继续接收键盘和原生粘贴，编辑区仍保留正常文本编辑行为。

实际 Electron 终端组件 25 个行为检查通过：原始 CRLF 与括号粘贴字节保留、真实编辑与回车不发送、两种模式使用编辑后内容、超过 12,000 字符的尾部可修改并完整发送、空内容禁用、键盘焦点循环、编辑草稿跨标签保留、底层输入隔离，以及取消／断线／重连和异步剪贴板隔离。编辑框截图核对、生产编译和 Windows 0.3.9 成品启动检查通过；测试使用隔离配置和模拟传输，不连接用户服务器。

## 0.3.8：修复快捷键误刷新工作区

生产窗口此前未显式配置 Electron 应用菜单，继承的 `reload`／`forceReload` 菜单快捷键与终端历史搜索、断线重连撞键。刷新会重建 React 页面并丢失内存中的标签与终端状态；后台 SSH Worker 不一定同时断开，因此表现为回到主页、所有已连接终端不可用。

Windows／Linux 显式移除默认应用菜单，macOS 仅保留系统应用、文本编辑及最小化等菜单，不包含刷新或强制刷新角色。修复位于主进程，不阻止终端接收原始按键，不改动用户的快捷键设置或单标签重连逻辑。

使用独立的可见且获得焦点的标准 Electron 窗口验证旧菜单的 Ctrl+Shift+R 与 Ctrl+R 能实际触发刷新；隐藏窗口的按键注入不能覆盖菜单加速键，无边框测试窗口中未用同样的注入方式复现旧行为，不将标准窗口实验当作用户旧版窗口的精确复现。生产菜单策略下 7 种组合键继续抵达页面，窗口不重新加载。实际 Gooeshell 界面 25 个行为检查通过，新增验证两个在线终端的标签、原终端实例、滚动内容和连接历史保留，主页的隐藏终端保留，以及设置草稿保留；现有断线后单标签重连继续通过。生产编译与 Windows 0.3.8 成品启动检查通过。测试不访问用户服务器或配置。

## 0.3.7：打包压缩传输

上传、下载、文件选择器、右键及拖拽入口共用传输方式选择框，普通传输为默认值。压缩模式每个选中项对应一个 tar.gz 任务，使用既有 SFTP 校验链路传输，自动解压后才标记完成。来源、连接和目标目录固定为操作开始时的快照；排队、压缩、校验、传输和解压状态均可取消，失败重试重新打包。关闭选择框不开始任务，连接断开后禁止提交。本地和远程列表移除额外的已选数量栏，保留选中高亮和现有工具栏／右键传输入口，点击列表空白处取消选中。

本地采用流式 tar/gzip，远端通过独立 SSH exec 运行固定 Python helper，路径通过 JSON 标准输入传入。完整校验压缩包和原始大小后才发布新目标，拒绝覆盖同名文件或空目录，拒绝路径越界、链接、特殊文件及冲突条目；清理仅操作本任务的临时目录。Windows 上传的目录映射为可遍历的 0755，普通／只读文件映射为 0644／0444；POSIX 保留常规权限，不带特殊权限位。跨阶段固定下载目录身份，监控压缩期间的数据通道关闭；远程准备阶段设单独超时，长文件压缩传输不使用该短超时。

验证包括本地压缩解压、乱码／长路径／空目录、损坏归档、取消和不覆盖竞态；真实 Linux Python helper 的打包解压、TERM 取消与临时目录清理；Windows Node 与 Linux Python 两个方向的归档互通。隔离 SFTP 服务器实测压缩数据上传下载、内容一致与自动解压（文件端点使用隔离本地适配器，远程 helper 由 Linux 测试单独验证）。最终默认与 SFTP 组合测试 198 项通过、25 项按环境跳过；远程 helper 专项 15 项全部通过。实际 Electron 文件管理 25 个行为检查通过，其中包含 8 个传输选择、阶段、重试与连接快照检查，以及两栏空白处取消选择、表头和滚动条不误取消。生产编译和 Windows 0.3.7 成品启动通过，成品直接加载包内压缩模块，验证中文文件与空目录压缩解压。测试不访问用户服务器或修改用户配置。

普通传输保留校验后续传，压缩传输不跨任务续传。同名目录不合并；远端需 Python 3 和不覆盖发布能力，sudo 传输尚未支持。双方需有临时空间；远端断线无法清理时，提示已知临时目录位置。跨平台构建结果以对应 GitHub Actions 记录为准。

## 0.3.6：文件列表列排序

本地和远程文件栏的名称、大小、修改时间列，以及远程权限列支持点击切换升降序；当前列显示方向箭头与 `aria-sort`。文件夹始终在前，元数据按数值比较，未知值置后，数值相同时用自然名称稳定排序。不改变原始文件数组和路径选择；两栏排序独立，刷新、导航和折叠后保留本次运行的排序。排序结果按条目和排序条件缓存，不额外读取服务器目录。

排序逻辑 6 项测试通过，覆盖自然数字名称、真实字节、时间、权限位、未知值、目录优先和输入不可变。实际 Electron 检查逐一点击四个远程列的升降序，验证箭头、实际行序、两栏独立、选中路径保留，以及刷新和折叠后的排序保留；连同原有 14 个文件操作检查通过。生产编译及 Windows 0.3.6 成品启动检查通过。

## 0.3.4：文件管理、多行粘贴和终端重绘

本地与远程工具栏及右键提供新建文件／文件夹、重命名、复制路径和多选删除。删除确认固定连接、路径和选择快照；部分删除后 sudo 重试跳过已经完成的项。远程递归删除使用 Python 目录句柄，不沿符号链接遍历，拒绝根目录和不明确的相对路径；原子独占创建保护同名文件。文件夹拖拽与传输入口使用既有递归传输后端。修复通知容器截获空白区域鼠标，以及文件区最小高度不足。

目录跟随仅轮询开启功能的当前可见终端，通过同一 SSH 连接的独立 exec 通道识别 Linux OpenSSH PTY 和 tmux 活动面板，不向终端发送 shell 命令。手动浏览、无法识别或权限不足时暂停。目录查询需 Python 3、可读取的 `/proc`，不支持递归追踪嵌套 SSH；目录递归删除也需要 Python 3。

键盘、系统及鼠标多行粘贴共用选择框。整体粘贴保留原文和 xterm 的括号粘贴行为；逐行模式先填入一行，用户提交后主动点击下一行，不自动把后续命令送给尚未完成的程序。取消、断线和重连会清除队列；切回选择框保留焦点，已切换标签的异步剪贴板读取不会继续发送。

从 xterm WebGL 缓存实现确认，同配置终端可能共享字形图集，而单终端清理图集不同时清理其他终端的字形坐标。现在通过独立图集身份隔离每个终端，继续共享字体文件；返回标签时刷新可见内容，不重建终端、连接或缓冲区。未稳定复现用户截图中的全部乱码，修复覆盖了已确认的共享缓存风险和切换重绘路径。

验证：文件操作 16 项（含真实 Linux 符号链接与替换竞态、Windows junction）、目录查询 5 项（含真实 SSH 通道、Linux PTY、两个 tmux 客户端）通过。实际 Electron 资源管理器 14 个行为检查、粘贴 15 个行为检查，以及 GPU 字形截图／隐藏字体变化／重复切换／缓冲保留检查通过。本机默认和 SFTP 组合检查 138 项通过、24 项按环境跳过，无失败。生产编译及 Windows 0.3.4 成品启动通过，成品通过真实 preload/main IPC 验证新建、同名拒绝、原内容保留和隔离目录删除。

测试均使用独立数据目录、模拟接口或本机测试服务，未访问用户服务器或修改用户配置。原生界面、粘贴与图集回归已加入四平台流水线，跨平台结果以对应构建记录为准。

## 0.3.3：连接入口与紧凑侧栏一致性

紧凑侧栏保留分组图标、短名称、数量、折叠状态和组菜单，与展开侧栏共用状态；新建分组入口继续可用。连接菜单统一为“新建同名终端”，显示目标地址、根据实际大小避让窗口边缘并支持方向键操作。侧栏在线和选中状态使用完整连接身份判断，同一连接有多个终端时优先保留当前匹配的终端。

终端标签菜单绑定具体会话，切换和重连不会跳到其他同名标签。已保存地址或跳板路径变化时，从旧标签新建终端保留旧目标并使用独立配置身份；旧标签的地址指纹属性也针对原地址，并提示编辑设置对应已保存的新配置。

现有完整 App、连接生命周期和连接界面三项原生回归通过，分别包含 22、24、8 个行为检查点。使用真实鼠标右键与命中检查验证展开／收起侧栏的新建同名终端入口，覆盖分组操作、原会话保留、精确重连、地址变更后的状态和指纹属性。生产编译通过；Windows 0.3.3 成品以隔离配置启动，验证真实分组展示和右键菜单可见、可点击，并核对截图。测试只使用示例连接、本地模拟接口和隔离配置，不访问用户服务器或修改用户设置。跨平台验证以该版本构建记录为准。

## 0.3.2：终端颜色与额外粗体

默认纯黑采用鲜明的 ANSI 配色，旧色保留为“柔和”。新增默认关闭的额外粗体选项；关闭时 xterm 的普通和强调均使用逻辑 400 槽，由现有组合字体继续映射用户选择的中英物理字重。开启后强调使用逻辑 700 槽。始终关闭粗体自动映射亮色，不过滤或改写终端输出。

配色与存储 7 项测试通过，验证旧配置默认值、布尔值持久化、配色有效性和可读性。现有原生配色检查在 GPU 与强制 DOM 两种模式均通过，比较实际像素／字形，确认普通色、明确指定的亮色和真彩色不因加粗改变，开关恢复后字形恢复；同一个终端实例、滚动内容与输入模式保留。

实际本地 tmux 渲染检查通过，中英文独立字重及手动选择的物理粗体仍有效。生产构建和 Windows 0.3.2 便携包启动通过，实际设置页检查默认配色与额外粗体开关的保存、重新打开和还原。所有检查使用隔离配置及本地测试终端，不修改用户服务器或应用配置。跨平台验证以该版本构建记录为准。

## 0.3.1：设置窗口稳定尺寸

仅为设置对话框指定稳定高度，保留视口高度约束。设置内容与栏目导航独立滚动，移除随内容变化的高度和短窗口最小高度；标题、页脚不参与滚动。切换栏目回到内容顶部，设置草稿仍由外层保存。

生产编译与 Windows 便携包构建通过。使用隔离配置启动实际打包程序，在 1280×860、960×640、960×430 三种视口下逐一切换全部六个栏目：同一视口中外框位置、宽高和内容宽度一致，长栏目可滚动，保存按钮始终可见且可点击。核对字体页和滚动到底部的快捷键页截图。跨平台验证以该版本构建记录为准。

## 0.3.0：高级连接与跳板机

新增单级 SSH 跳板配置，目标与跳板认证、密码保存及指纹确认相互独立。目标配置保存经过白名单清理的跳板快照；更换跳转路径改变目标凭据身份，已有直连身份保持兼容。跳板密码保存在独立命名空间，显式清除共享跳板密码不会清除目标密码，也不会在尚未完成的认证后被重新保存。

所有目标连接经 `direct-tcpip` 转发，包括终端、SFTP、编辑和传输；配置跳板后不存在直连回退。复用按跳板地址、身份、认证材料及信任策略隔离，共享连接按目标引用释放。界面支持展开高级选项、复制已用跳板设置、分别输入两端密码、取消连接，以及较矮窗口中的滚动表单和固定操作按钮。

Windows 完整默认测试 118 项通过、0 失败、21 项按环境跳过；其中专门跳板机测试 11 项通过，验证实际终端与 SFTP 路径、断点上传下载、等待传输确认期间的终端往返、共享／独立连接数量、错误认证、拒绝转发、取消、跳板断线与重连。实际主进程 IPC 保留原有 16 项检查并通过 8 项跳板机检查；连接界面、认证生命周期和完整 App 三项原生界面回归通过。生产构建通过。

使用本机隔离 SSH 服务验证，不连接用户服务器。这里只支持允许 TCP 转发的普通 SSH 跳板机，交互式资产菜单与多级跳转不在本版范围内。公网延迟与吞吐不作承诺；跨平台构建和原生验证以该版本构建记录为准。

## 0.2.0 正式版

版本号统一为 `0.2.0`，移除界面 PREVIEW 标记与预览版称谓；保留既有连接和设置的数据目录。补充配置、系统加密凭据、指纹及私钥路径的备份说明。功能基于 `preview.15`，其 Windows x64、macOS arm64 / x64、Linux x64 构建及原生测试均已通过。正式包的校验和、源提交和该次构建地址随发布包提供；平台验证结果以这些记录指向的 Actions 任务为准。

## 0.2.0-preview.15：精简主页与服务器条目

新标签页仅列出最近连接，移除重复的已保存服务器区域；收藏仍在侧栏，历史显示开关继续生效。展开侧栏的服务器条目去掉前置图标，采用中性细边框、圆角与间距区分，名称和地址保持两级文字层次。折叠侧栏时保留可点击图标，分组图标不受影响。

沿用实际 App 与连接界面回归，调整已有检查与点击入口，验证最近连接原位打开、仅保存更新侧栏、折叠侧栏可用，以及既有标签关闭、取消和重连行为。

两项现有 Electron 界面回归通过，生产构建通过。Windows `preview.15` 便携包已使用隔离配置实际启动，验证主页、字体目录及桌面接口，并用三个演示连接检查黑白主题下同组条目间距、圆角、文字层次和图标显示；未连接用户服务器。

## 0.2.0-preview.14：独立新标签页

标签栏“＋”创建独立选择页，不打开 SSH 表单或建立连接。主页分别显示已保存服务器与可关闭的最近连接；从主页选择服务器会在该页所在标签建立终端，即使同一服务器已在其他标签打开。侧栏单击仍优先复用已有连接。快速连接保持原快捷键；只保存配置或取消连接均保留选择页。

标签顺序独立于 SSH 会话，现有 xterm 实例切到选择页时保持挂载，前后切换包含选择页。连接过程绑定最初标签；后台成功不抢焦点，关闭目标后取消并断开晚到的会话，不恢复已关闭标签。关闭最后一页创建新选择页，远程文件、传输和命令接口仅使用实际会话 ID。

实际 Electron 生命周期夹具与完整 App 回归覆盖多页新增、已保存/最近连接原位打开、保留终端实例、键盘切换、连续关闭、取消晚到连接、仅保存不连接和最后一页关闭；既有重连、命令栏与配色检查继续执行。平台构建结果以对应 GitHub Actions 任务为准。

Windows 默认测试 100 项通过、0 失败、21 项按环境跳过；实际生命周期夹具 20 项行为检查、完整 App 19 项行为检查通过。修复了同一批事件中连续选择、关闭多个标签时的旧选中状态覆盖。`preview.14` 便携包已实际启动，验证“＋”只新增选择页、已保存配置可见、关闭页数正确、取消快速连接保留主页；使用隔离配置，不连接用户服务器。

## 0.2.0-preview.13：命令管理与独立终端配色

右侧命令栏默认收起，支持全局与按连接保存的分组、搜索、排序、复制和编辑。命令库独立原子保存，连接改名、关闭标签和删除连接不会清理命令。借用其他连接的命令先显示来源、目标和正文；预览期间切换终端、断开、修改命令或适用范围后禁止发送。现有终端与已修改地址的连接配置不视为同一个命令目标。

命令发送绑定实际 SSH 会话，填入不追加回车、运行追加回车。读取实际 xterm 括号粘贴模式，多行与 Tab 在未启用此模式时拒绝发送；拒绝终端控制字符和目标编码不能无损表示的文本。实际 Electron 主进程、preload 与本机 SSH 回归检查增删改、持久化、过期请求、跨连接借用、重连及准确输入字节。

新增“设置 → 终端配色”，支持跟随界面及五种独立配色。实际 xterm 回归在默认与强制 DOM 回退两种渲染路径检查同一终端实例、滚动内容、尺寸、备用屏幕与输入模式保留，背景图片继续显示，配色不改变真彩色输出。完整 App 回归检查快捷键展开/收起命令栏、命令持久化，以及黑白界面下 1280 和 960 宽窗口的布局。

同一配置目录采用 Electron 单实例锁，第二次启动不初始化存储、连接服务或窗口，而是唤回已有窗口。真实多进程回归检查重复实例退出、已有窗口恢复路径和不同配置目录可独立运行。Windows 最终默认测试 100 项通过、0 失败、21 项按环境跳过；额外启用命令与配色回归 5 项、连接回归 6 项、编辑器回归 2 项、真实 SSH/tmux 回归 2 项全部通过。`preview.13` Windows 便携包已实际启动并通过主页、字体和桌面接口检查。

上一版 CI 暴露的 macOS 输入选择与首次布局时序问题已修正测试夹具；重连渲染检查兼容 GPU 不可用时的实际 DOM 绘制，同时保留可见内容、渲染事件和按钮遮挡检查。新增跨平台 `ci-command-tests.mjs`，既有连接、编辑器与打包启动验证继续执行；macOS 和 Linux 构建及验收结果以对应 Actions 任务为准。

所有测试使用临时数据、隔离窗口和本机服务，不连接用户服务器。命令库不加密，凭据应使用专门的系统加密密码功能；命令发送进入当前交互程序，不自动识别 Shell 提示符。远程文件分类颜色仍取决于服务器输出，应用不会修改服务器配置。外部编辑器联动继续后置。

## 0.2.0-preview.12：连接管理、密码与重连

新增统一连接目录迁移、收藏与历史分别删除、分组排序、保存属性无需连接、认证成功时间与配置变更并发保护。密码不进入连接配置；连接身份包含地址、端口、账号、认证方式及私钥路径。长期凭据通过主进程的系统加密适配器保存，不向界面返回密码。

实际 React/Electron 回归覆盖分组与图标、最近连接右键编辑、单独保存、身份验证弹窗、取消连接、同标签重连和后台重连不抢焦点。sudo 保存期间切换标签会停止发送，不向新标签发送旧目标密码。保存地址变化后，现有终端仍绑定原目标，原目标重连使用独立连接标识。

实际 xterm 重连回归验证三个 SSH 会话共用同一个终端实例、90 行普通终端输出全部保留、旧备用屏幕及鼠标/粘贴模式复位、新会话收到尺寸与输入、旧输出及延迟确认不会串到新会话。修复了软复位回到屏幕顶部后断线提示覆盖旧输出的问题。重连快捷键仅在断线时拦截。

实际本机 SSH → Linux PTY → tmux 的启动、分屏、字体、主题、持续输出回归通过（2 项）；内置编辑器界面及真实主进程关闭保护回归通过（2 项）。所有测试使用隔离数据和本机测试服务，不连接用户服务器。

跨平台连接界面与主进程回归由 `desktop/scripts/ci-connection-tests.mjs` 执行；原有编辑器与打包启动验证继续执行。macOS / Linux 的原生构建以对应 GitHub Actions 运行结果为准。公开网络输入延迟、各发行版桌面密钥环及所有 sudo 配置未穷举验证。

本轮最终 Windows 默认测试为 88 项通过、0 失败、15 项按环境跳过；另外启用的连接整合回归 5 项全部通过。Electron 44.3.0 的实际 Windows 异步系统加密往返验证通过；完整主进程 IPC 使用独立加密夹具验证凭据清除、身份往返变更、删除连接与认证并发、工作进程异常恢复。Windows `preview.12` 便携包已启动验证主页、字体目录和本地接口正常。

当前图形版位于 desktop/。Windows 类型检查和生产构建、实际 Electron 窗口启动已经通过。真实本机 SSH 测试验证了指纹确认之前不发送密码、拒绝未知/变化指纹、保存指纹、初始提示符、输入回显及会话关闭。

0.2.0-preview.2 在 Windows + Ubuntu WSL 的完整本机测试中 27 项通过，0 失败、0 跳过。另验证了非 UTF-8 拒绝有损编辑、已有本地目标不覆盖、连续设置保存和并发主机配置读改写。

真实 SSH/SFTP 测试验证了上传下载一致、双向非对齐前缀续传、错误前缀保护、已有目标不覆盖、取消后续传、Unicode/空目录递归与断连取消不挂起。

Ubuntu 实际文件测试验证了目录/文本/原子重命名、符号链接提权写入拒绝、权限位保留、shebang执行及时间/输出限制。sudo协议测试验证了密码发送时序、NOPASSWD不发送密码、错误密码不重复发送、requiretty/Python缺失与取消。真实sudo密码通过模拟SSH通道验证，未改动系统sudoers或连接用户服务器。

终端流控测试验证 1 MiB 原始字节逐字节一致、达到阈值暂停、收到解析确认后恢复、旧鼠标协议字节转发和重复尺寸去重。真实本机 SSH → Linux PTY → tmux 测试通过启动、分屏、137×41 尺寸更新和约 2 MB 连续输出。测试使用独立 tmux socket，不操作用户服务器或现有 tmux。

独立 Electron 完整渲染回归另行通过：使用实际 TerminalView/xterm 和本机 SSH，验证 DA/DA2/光标位置回应、tmux 备用屏幕、分屏及约 2 MB 输出后的继续输入；等待 onRender 与画面合成后，截图实际显示最终输出。测试使用独立浏览器数据目录，不覆盖用户远端 tmux 配置，也不作为输入延迟基准。

文件栏使用真实组件与样式做了隔离 Electron 布局验证：单击前后行 y 坐标均为 632，原坐标双击进入正确目录；两栏可单独或同时折叠为 34 px，恢复后目录 DOM、选中项与滚动位置保持不变。

0.2.0-preview.3 增加传输队列独立折叠。本地浏览器实际验证：队列由 102 px 收起为 29 px，文件列表由 196 px 增至 269 px，整个文件区高度保持不变。

0.2.0-preview.5 将中英文字体控件改为原生下拉选择，取消按当前字体名过滤候选。浏览器交互验证了完整选项保留、End/Enter 选择、保存后重新打开恢复中英文选择，以及 Esc 仅收起展开的字体列表。已保存但未在本次枚举中的字体仍保留为选项。

0.2.0-preview.6 统一为中性黑白主题和圆角界面。浏览器验证深浅主题的主界面、设置与连接弹窗、顶部切换、保存后重新打开和取消不应用；原生字体列表仍可使用 End/Enter 选择完整候选。选中文件前后行坐标不变，本地/远程栏分别收起为 34 px，队列由 102 px 收起为 29 px。旧配置缺少或含无效主题时使用黑色，白色主题保存后重新读取保留字体和快捷键；相关设置/本地文件测试 7 项通过。

本版实际 Electron/xterm/tmux 回归验证黑→白→黑使用同一终端实例，备用屏幕和 131×39 尺寸不变，切换后继续输入、分屏、连续输出和解析确认均通过。浅色终端截图检查确认绘制区域为白底，修复了 xterm 默认 viewport 黑底残留；合成截图像素断言防止只更改外层背景而遗漏真实终端。

0.2.0-preview.7 默认收起整个文件区、本地栏及传输队列。浏览器交互验证展开后本地栏为 34 px、队列为 29 px，远程栏可用；文件按钮默认只显示图标，设置可显示文字。最近连接与收藏分开，关闭主页历史显示后立即隐藏，快速连接两台演示主机不会增加侧栏收藏。Ctrl+Shift+F1 打开设置、Ctrl+Shift+P 快速连接、Ctrl+Shift+左右方向键循环切换标签并聚焦终端。旧版自定义键位占用新增切换键时保留旧动作，新动作显示未设置；空绑定不拦截单独修饰键或鼠标左键。

连接右键属性可按地址和端口开启自动接受指纹，包括收藏、最近连接和终端标签；不添加收藏，也不修改连接原本的记忆指纹偏好。实际本机 SSH 测试验证自动模式不读取或写入指纹、不触发确认，错误密码仍拒绝；下一次新连接允许更换设备，同次会话后续文件连接拒绝指纹变化。关闭属性后恢复原规则；普通临时信任仍需确认。存储测试验证地址/端口隔离、非法数据清洗和并发更新。

Windows 默认测试命令本次为 35 项通过、0 失败、4 项按环境跳过（Linux helper、真实 SFTP、独立 tmux 和完整渲染）。另行运行的实际 Electron/xterm/SSH/tmux 渲染回归通过：普通 DejaVu → JetBrains Mono 粗体 → 普通 DejaVu 保持同一终端，字体切换前后为 131 列；普通和粗体均加载真实字体文件，主题切换、持续输入、分屏、大量连续输出和解析确认正常。已修复首次选择粗体时普通字形尚未加载导致终端网格测量错误。字体资源测试检查内置三个字体族的名称与 400/700 字重；推荐列表仅包含可用字体。

用户报告的偶发 tmux 画面停留尚未稳定复现；输入与渲染修复不能视作根因已经确认。自动构建结果见本仓库 Actions。

0.2.0-preview.8 增加独立英文字重与中文字重。默认测试为 40 项通过、0 失败、5 项按环境跳过。旧版共用 700 会迁移为英文 700 / 中文 700，分别保存 400 / 290 后重新读取保留；非法字重不会进入配置。字体分区测试检查汉字、全角标点与 ASCII/终端线框没有范围重叠。

另行运行的 Windows 字体目录测试 4 项通过，实读 TTF/TTC 名称表，正确选择微软雅黑 UI 的 TTC 子字体并过滤 WPF 合成字形。本机微软雅黑实际提供 290/400/700，宋体只有 400；不把系统模拟粗体列为可选原生字重。浏览器操作验证英文 400 / 中文 700 保存后独立恢复，更换宋体后只显示 400 并给出单字重说明。

实际 Electron/SSH/tmux 渲染回归在固定 DPR 1 下通过两组像素比较：英文使用 DejaVu Sans Mono 或微软雅黑时，单独把中文 400 改为 700，英文行像素完全不变，中文墨量增加约 24.5%。终端实例、行列、主题、后续输入、分屏、大量输出及 ACK 保持正常。测试也要求首次尺寸通知先于完整字体目录返回，防止首屏等待系统扫描。非整数 DPI 下存在 xterm 栅格取整带来的像素位置差异，仍保留完整 DPI 验收的边界。对于仅提供普通字重的字体，不注册虚假的粗体片段，命令输出的 ANSI 粗体可正常使用浏览器合成效果。

本地便携包使用隔离 Electron 窗口实际加载打包后的页面、preload、字体目录和资源，通过中文 290/400/700 列表、英文 400 / 中文 700 保存重开、宋体仅 400 及 ANSI 粗体墨量增加检查，无渲染错误；没有连接用户服务器或读取用户连接配置。

0.2.0-preview.9 增加 `Ctrl+Shift+[` 折叠 / 展开侧边栏，设置页支持重新绑定，按钮提示随状态和键位更新。浏览器演示验证主页与终端聚焦时均能连续折叠、展开，终端输入焦点保持，单独输入 `[` 不触发；设置页重新录入组合键显示为 `Ctrl + Shift + [`，录入时不触发侧栏动作。按住 Shift 产生的花括号按物理方括号键统一识别；配置测试覆盖旧花括号键位迁移、新键冲突保留旧动作，以及自定义和清空绑定的保存。

同版增加 `Ctrl+Shift+]` 独立控制终端标题及标签栏。纯终端模式默认保留两栏，隐藏后终端填满剩余高度；模式切换不会重置其可见状态。浏览器演示实测普通与纯终端模式反复显示、隐藏，标题/标签高度由 48/38 px 变为 0/0 px，输入焦点保持；从标签栏隐藏时焦点也会回到终端，纯终端模式点击文件按钮可正常退出并展开文件区。配置测试覆盖右方括号的旧绑定冲突与保存迁移。默认测试 41 项通过、0 失败、5 项按环境跳过。

本地便携包通过隔离 Electron 窗口加载实际打包页面及 preload，验证普通窗口、纯终端、全屏加纯终端、全屏四种状态的标题栏隐藏/恢复与高度不溢出，同时检查侧栏快捷键。全屏通过真实 BrowserWindow 切换；无渲染错误，没有连接用户服务器或读取用户配置。

0.2.0-preview.10 增加 macOS arm64/x64 和 Linux x64 原生构建配置，Windows x64 继续提供便携包。系统字体枚举在 macOS/Linux 读取 OpenType/TTC 名称、OS/2 字重和样式，Windows 原有 WPF 路径保留；首次设置按本机已安装中文字体选择，无匹配时使用 CSS 系统回退。新增测试验证真实内置 TTF、双字体 TTC、字重和样式、通用字体 CSS 语法、POSIX 根目录及 Windows 盘符目录。Windows 本地完整测试 45 项通过、0 失败、6 项按环境跳过，两端类型检查通过。

跨平台构建流程在对应系统运行 SSH/SFTP 回环测试、目录及配置测试、系统字体检查，并启动实际打包应用验证主页与桌面接口。启动检查使用独立用户目录；Linux CI 通过虚拟显示服务运行，CI 专用启动参数不写入产品默认配置。各平台结果可在 gooeshell Desktop 的对应构建任务查看；Release 校验清单与构建信息关联源提交。macOS 采用 ad-hoc 签名，尚未完成 Developer ID 签名和公证；新平台尚无与 Windows 同等范围的人工实机体验验收。

0.2.0-preview.11 优先加强内置文本编辑器。CodeMirror 6 在首次打开文件时单独加载，保留终端主界面的加载路径。新增行号、语法高亮、代码折叠、多光标、查找替换与正则、跳转行、撤销重做、手动语法选择、2/4 空格或 Tab 缩进、JSON 格式化、展开编辑器和自动换行。支持 UTF-8、UTF-8 BOM、UTF-16 LE/BE、GB18030、Big5；无损保留 BOM、LF/CRLF/CR 和混合换行，转换编码无法表示字符时拒绝保存。

本地完整测试 59 项通过、0 失败、10 项按环境跳过。另行运行真实本机 SFTP 编辑测试和 WSL Linux helper 测试通过，验证打开时版本核对、同尺寸内容变更、删除、并发保存、权限/所有者保留、普通与 sudo 读写兼容、超限只读预览。修复旧读写接口及新编辑接口一次 SFTP 读取过大可能导致连接关闭的问题，改为分块读取。文本保存使用临时文件后替换，版本检查没有绕过按钮；再次覆盖比较版本时也会重新核对。保存完整 ACL/扩展属性、远程程序同时写入与原子替换之间的无条件竞争窗口仍不承诺完全解决。

真实 Electron 编辑器回归通过 12 项行为检查：Ctrl+S 恰好提交一次、撤销重做、查找替换、固定原连接、即时关闭保护、冲突保留草稿和再次核对最新版本、断线保留内容、本地副本、保存期间继续编辑、保存并关闭期间新增修改保留、JSON 格式化及无效 JSON 不改动。发现并修复中文输入尚未提交到编辑状态时快速关闭的问题，保存与关闭前通过公开的编辑器测量周期同步输入。检查黑白界面实际截图，浅色编辑区像素为纯白，无渲染错误。

真实 Electron 主进程使用隔离配置验证新读写接口、冲突错误、本地副本取消/覆盖及跨编码覆盖；取消原生窗口关闭或退出时不会终止连接服务，保存忙碌时阻止关闭，明确放弃后正常退出。既有真实 Electron/xterm/tmux 渲染和独立 tmux 测试再次通过，包括主题/字重切换、持续输入、分屏和窗口尺寸变化。自动构建在各平台打包后新增编辑器与原生关闭回归，Linux 使用隔离虚拟显示环境。

当前编辑器一次打开一个文件，最大 2 MiB；断线稿保留于当前窗口，可以另存到本地，尚无崩溃后自动恢复或外部编辑器联动。此次不扩展连接分组、凭据长期保存、命令侧栏和重连功能。

## 尚未完成的产品功能

- 跨软件一致的字号/字体像素级匹配与完整 DPI 验收。
- 端到端输入延迟、更多服务器、输入法和拖拽边缘情况验收。
- 自动从远程 Shell 收集结构化命令历史。
- 一键部署公钥。
- sudo大文件传输、远程文件直接拖出到外部Explorer、SSH跳板与配置别名、本地Shell。
- 完整后台传输队列与应用重启后的任务列表恢复。
- 与 Windows Terminal / Xshell 的实际按键到屏幕延迟对比。

这些项目仍保留在设计中，不作为首版已完成能力。
