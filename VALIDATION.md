# 验证与已知边界

当前构建和验证正在进行，完成后更新本文件。

## 尚未完成的产品功能

- 跨软件一致的字号/字体像素级匹配与完整 DPI 验收。
- 快捷键任意组合键的直接捕捉录入；首版使用组合键文本编辑。
- 自动从远程 Shell 收集结构化命令历史。
- 一键部署公钥。
- 传统终端编码切换。
- 完整后台传输队列与应用重启后的任务列表恢复。
- 与 Windows Terminal / Xshell 的实际按键到屏幕延迟对比。

这些项目仍保留在设计中，不作为首版已完成能力。
