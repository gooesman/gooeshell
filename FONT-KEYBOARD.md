# 字体与快捷键设计

状态：功能设计，尚未实现。用户要求参考本地 FinalShell 和 Xshell 的字体，并重点做好快捷键。

## 参考与只读核实

- 用户截图一：DejaVu Sans Mono、Normal、字号 9；列表包含系统字体与多种等宽字体。
- 用户截图二：字号 15；英文字体和中文字体分开选择，顶部显示中英文预览。两个软件显示的字号可能受单位、DPI 和渲染差异影响，不直接认为 9 与 15 是相同大小。
- 读取 D:/finalshell/finalshell.jar 的 ZIP 资源目录，确认存在 DejaVu Sans Mono、IBM Plex Mono、Inconsolata、JetBrains Mono、Nanum Gothic Coding、Noto Sans Mono、Nova Mono、PT Mono、Roboto Mono、Share Tech Mono、Source Code Pro、Space Mono、Ubuntu Mono 等字体文件；还有资源文件名 Consola.ttf。
- 在 D:/xshell7 中未找到独立的 ttf/otf/ttc 文件；这个结果不证明其二进制内部没有字体资源。
- Windows 字体注册信息确认有 DejaVu Sans Mono 及其粗体/斜体、Consolas、Microsoft YaHei、SimSun/NSimSun、Noto Sans SC 等。系统字体注册与应用私有字体不是同一个列表。
- 本轮只列出字体资源与系统字体信息，未读取主机连接配置、凭据或命令历史，未提取或修改参考软件文件。

## 字体体验

1. 字体设置采用“英文/数字字体”和“中文字体”两个选择器，附统一字号、字重和实时预览。字体家族名优先展示，字重单列，不要求用户识别 ttf 文件名。
2. 初始预览以 DejaVu Sans Mono Regular 为英文参考；缺失时回退 Consolas。中文先跟随系统回退，也允许显式选择新宋体、微软雅黑、Noto Sans SC 等本机可用字体。未替用户锁定最终中文字体或字号。
3. 字体源为系统字体、应用内置字体和用户导入字体。应用内置资源在开发时从可再分发的上游来源取得，保留许可证；参考软件包中的字体只用于确定参考家族。
4. 支持搜索、等宽字体优先、字重筛选和字体缺失提示。中文不按西文等宽元数据直接排除；默认隐藏带 @ 的竖排字体。
5. 预览覆盖英文、数字、中文、标点、0/O、1/l/I、粗体、框线和常见终端符号；提供纯黑背景预览。
6. 字号用明确的 pt 单位，可输入小数；支持实时增减和重置。DPI 变化后保持逻辑字号，重新计算像素与网格。
7. 字体调整时保持选择/滚动状态的合理行为，并更新远程 PTY 尺寸。检验中文宽度、tmux 框线、Vim 光标和不同缩放比例下的对齐。
8. 全局默认可被单连接覆盖；保存前可预览、取消恢复。高级选项包含行高、字符间距、连字开关和符号回退，初始界面不强迫调整这些选项。

## 快捷键匹配原则

- 一个动作有稳定 ID，按键只是绑定；支持多个绑定、修改、清除、恢复单项及全部默认。
- 设置页可按动作名或实际按键搜索，支持直接录入组合键；录入模式捕获按键，不向服务器发送。
- 区分终端、文件列表、文本输入框、对话框等上下文。不同上下文允许复用同一按键；会同时匹配的冲突必须显示，不能静默覆盖。
- 输入法组合期间优先由输入法处理文字候选和确认；尊重 AltGr、键盘布局、左右修饰键与自动重复。
- 默认终端 Ctrl+C 中断，Ctrl+R 交给远程 Shell，Ctrl+L/Ctrl+A/Ctrl+W 及 Alt 组合等常用 Shell/Vim 键不擅自占用。
- 本地弹窗打开时按弹窗规则处理；编辑框按常规文本编辑规则处理；其他情况按显式绑定决定是否执行应用动作，未匹配按键立刻转发终端。
- 初版不启用需要等待后续按键的应用级前缀序列，普通输入不因快捷键识别增加超时；为 tmux 保留默认 Ctrl+B。
- Esc 默认用于终端或关闭当前弹窗，不单独用于退出全屏，避免影响 Vim。
- 全部栏隐藏时仍可用键盘恢复布局；提供可搜索动作面板和可见的快捷键帮助入口。
- 能向用户解释被系统保留、无法可靠捕获的组合键；同名配置导入时预览变更和冲突，不静默清空已有设置。

## 初始默认绑定草案

用户可全部修改。终端绑定即使与某些远程程序的扩展按键重叠，也可解绑后直通。

| 上下文 | 按键 | 动作 |
| --- | --- | --- |
| 终端 | Ctrl+Shift+C / Ctrl+Insert | 复制选中文本；无选中时不产生副作用 |
| 终端 | Ctrl+Shift+V / Shift+Insert | 粘贴；尊重远程 bracketed paste 模式 |
| 终端 | Ctrl+C | 直接发送中断，不以是否选中文字改变语义 |
| 终端/文件列表 | Ctrl+Shift+P | 打开可搜索动作面板 |
| 终端 | Ctrl+Shift+F | 搜索终端回滚内容 |
| 终端 | Ctrl+Shift+H | 打开命令历史；选中历史默认填入，确认后才执行 |
| 主窗口，无输入法组合/模态弹窗 | Ctrl+Shift+E | 显示/隐藏文件面板；显示时聚焦，隐藏后返回终端 |
| 主窗口，无输入法组合/模态弹窗 | F11 | 切换无边框全屏，退出时恢复先前窗口状态 |
| 主窗口，无输入法组合/模态弹窗 | Ctrl+Shift+F11 | 切换纯终端布局，再按恢复之前各栏状态 |
| 终端 | Ctrl+Shift+= / Ctrl+Shift+- | 放大/缩小字体；按实际键盘布局录入并测试 |
| 终端 | Ctrl+Shift+0 | 重置到该连接配置字号 |
| 终端/文件列表 | Ctrl+Tab / Ctrl+Shift+Tab | 切换下一/上一会话标签 |
| 文件列表 | Ctrl+C / Ctrl+V | 复制选中项/粘贴到当前目录；冲突先选择策略；明确支持范围 |
| 文件列表 | F2 | 重命名选中项 |
| 文件列表 | F5 | 刷新当前目录 |
| 文件列表 | Delete | 发起删除操作，显示所选目标确认，不直接永久删除 |
| 文件列表 | Alt+Up | 上一级目录 |
| 文件列表 | Ctrl+L | 聚焦路径输入框 |
| 文件列表 | Enter | 进入目录；文件打开按后续明确的查看/编辑规则执行 |
| 文本输入框 | Ctrl+C / Ctrl+V / Ctrl+A | 常规文本复制、粘贴、全选 |

## 鼠标、粘贴与低延迟

- 选择即复制可开关；右键默认上下文菜单，可选直接粘贴。
- 远程程序启用鼠标协议时按终端规则转发，提供显式修饰键进行本地选择；上下文菜单或粘贴不能无条件吞掉远程鼠标事件。
- 大段粘贴有独立队列及取消能力，支持可配置的多行预览；不能堵住渲染或阻止用户取消。
- 快捷键匹配使用内存中索引，不在按键路径读文件、写数据库或等待网络。
- 修改绑定原子替换配置；复制失败、剪贴板暂忙或空内容给出轻量反馈，保持终端可输入。

## 验收场景

- 终端和文件列表中分别测试 Ctrl+C，含有/无选中、中文输入法组合及打开弹窗的情况。
- Bash/Zsh 中的 Ctrl+R/Ctrl+A/Ctrl+W、tmux Ctrl+B、Vim Esc/方向键等保持预期。
- F11 反复进入退出全屏；所有栏隐藏后可恢复；切换显示器/DPI 后窗口可用。
- 多种键盘布局下验证加减字号、AltGr、录入与冲突提示；长按只重复允许重复的动作。
- 粘贴多行、中文、大文本时不卡住界面；历史填入不会未经确认立即执行。
- 字体预览、取消、单连接覆盖、字体缺失回退和应用重启后恢复行为一致。
